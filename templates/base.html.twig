<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="NoS1gnal" />
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle`
										           and uncomment the following Encore helpers to start using Symfony UX #}
		{% block stylesheets %}
			<link rel="icon" type="image/x-icon" href="{{asset('img/pawStart1.png')}} ">
			<link rel="stylesheet" href="{{asset('css/style.css')}} ">
			<link rel="stylesheet" href="{{asset('css/accueil.css')}} ">
			<link rel="stylesheet" href="{{asset('css/barnav.css')}} ">
			<link rel="stylesheet" href="{{asset('css/accueil.css')}} ">
			<link rel="stylesheet" href="{{asset('css/carousel.css')}} ">
			<link rel="stylesheet" href="{{asset('css/intro.css')}} ">
			<link rel="stylesheet" href="{{asset('css/menu_circle.css')}} ">
			{# Bootstrap 5.1 #}
			<link rel="stylesheet" href="{{asset('css/bootstrap.min.css')}} ">
			{# fontAwesome #}
			<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">


		{% endblock %}


	</head>
	<body>
		{# Si on est sur ailleurs que sur la page de connexion alors on affiche la navbar #}
		{% if (app.request.pathinfo | striptags | slice(1, 5) == "") %}
			{% include "shared/_navbar.html.twig" %}
		{% elseif (app.request.pathinfo | striptags | slice(1, 5) == "story")%}
			{% include "shared/_navbar.html.twig" %}
		{% elseif (app.request.pathinfo | striptags | slice(1, 5) == "meteo")%}
			{% include "shared/_navbar.html.twig" %}
		{% elseif (app.request.pathinfo | striptags | slice(1, 4) == "list")%}
			{% include "shared/_navbar.html.twig" %}
		{% elseif (app.request.pathinfo | striptags | slice(1, 4) == "note")%}
			{% include "shared/_navbar.html.twig" %}
		{% elseif (app.request.pathinfo | striptags | slice(1, 5) == "admin")%}
			{% include "shared/_navbar.html.twig" %}
		{% endif %}


		{% block body %}{% endblock %}

		{# si on est sur un lien commençant par story on affiche les btn de slide #}
		{# exemple pour avoir la route #}
		{# {{ app.request.uri }} <br>
		{{ app.request.pathinfo }} <br>
		{{ app.request.requesturi }} #}
		{% if (app.request.pathinfo | striptags | slice(1, 5) == "story")  %}
			{% include "shared/_carousel_btn_footer.html.twig" %}
		{% endif %}


		{% block javascripts %}
			<script src="{{asset('js/jquery.js')}} "></script>
			<script src="{{asset('js/bootstrap.bundle.min.js')}} "></script>
			<script src="{{asset('js/script.js')}} "></script>
		{% endblock %}
	</body>
</html>
